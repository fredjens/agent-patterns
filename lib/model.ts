// Single source of truth for the model used by every pattern.
// Imported by both server (claude-client) and client (PromptPanel, footer).
// Change these two values to swap models everywhere.
export const MODEL_ID = "claude-haiku-4-5-20251001";
export const MODEL_LABEL = "Claude Haiku 4.5";
